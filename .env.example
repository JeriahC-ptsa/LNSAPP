# Environment Variables for LnSapp

# Security
SECRET_KEY=your-super-secret-key-change-this-in-production

# Database (PostgreSQL for production, SQLite for local development)
# For local development, leave commented to use SQLite
# For production (Render), this is automatically set by Render
# DATABASE_URL=postgresql://username:password@localhost:5432/lnsapp_db

# Flask Environment
FLASK_ENV=development
# FLASK_ENV=production  # Use this for production deployment

# Optional: Set to 1 for debug mode (never use in production)
# FLASK_DEBUG=0
